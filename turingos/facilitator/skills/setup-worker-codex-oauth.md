# Setup Worker: Codex (OAuth)

Workers execute capsules. Registry: `codex`, `claude`, `grok`, `fake_command`, `api`.

Codex: ensure `codex` CLI in PATH; OAuth via provider docs.
Dispatch: `turing dispatch <capsule_id> --worker codex`
TuringOS whitebox: sandbox, timeout, receipts on Micro tape.