# Credentials (Software 3.0)

- Env vars primary for CI/fluidity
- Persisted secrets: OS keyring only (`turing config --meta`)
- Micro events record MetaAIConfigured without secret bytes
- Facilitator never logs or displays api_key