# Adopt Existing Project

When user has `.git` + optional GitHub remote:
1. Propose `IntentCaptured` + `MacroObservationImported` with `macro:git:<pid>:<HEAD>`
2. Optional `WorkCapsuleBuilt` explore capsule (read-only)
3. Human approves before dispatch
CLI: `turing adopt .`