# Setup Meta AI (OpenAI-compatible)

1. `turing config --meta` (keyring) OR env:
   - `TURINGOS_META_BASE_URL`
   - `TURINGOS_META_API_KEY`
   - `TURINGOS_META_MODEL`
2. Meta AI proposes WorkOrders; separate from Facilitator.
3. Never store raw keys in JSON files.